!true              // unaria ← !
5 + 3              // binaria← +
edad >= 18 ? "mayor" : "menor"  //ternaria ← ?:
typeof "hola"      // ← unaria typeof
10 % 3             // ← binaria %
-5                 // ← - unaria (ojo: no es resta)
a && b             // ← binaria &&